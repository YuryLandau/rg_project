# 🗺️ Mapa Visual da Estrutura de Componentes

## 🌳 Árvore de Componentes

```
RG_Website
│
├── 📁 src/
│   ├── 📁 components/
│   │   │
│   │   ├── 📁 layout/          [Componentes de estrutura base]
│   │   │   └── 📄 Head.tsx     → <head> metadados
│   │   │
│   │   ├── 📁 sections/        [Seções principais da página]
│   │   │   ├── 📄 Header.tsx   → Navegação principal
│   │   │   ├── 📄 HeroSection  → Banner/destaque
│   │   │   └── 📄 Footer.tsx   → Rodapé
│   │   │
│   │   ├── 📁 ui/              [Componentes reutilizáveis]
│   │   │   ├── 📄 Button       → Botões
│   │   │   ├── 📄 Image        → Imagens otimizadas
│   │   │   ├── 📄 Container    → Wrapper de conteúdo
│   │   │   └── 📄 Section      → Seções de página
│   │   │
│   │   └── 📄 index.ts         → Exportações centralizadas
│   │
│   ├── 📄 App.tsx              → Composição principal
│   ├── 📄 main.tsx             → Entry point
│   └── 📄 index.css            → Estilos globais
│
└── 📄 COMPONENTES.md           → Documentação completa
```

## 🎨 Hierarquia Visual

```
┌─────────────────────────────────────────────┐
│  📱 App (Aplicação Principal)              │
│  ┌───────────────────────────────────────┐ │
│  │  🧠 <Head />                          │ │
│  │  [Metadados invisíveis]               │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  🎯 <Header />                        │ │
│  │  ┌─────┬─────────────────┬─────────┐ │ │
│  │  │Logo │  Navigation     │ Actions │ │ │
│  │  └─────┴─────────────────┴─────────┘ │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  🎬 <HeroSection />                   │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  Background Image/Video         │ │ │
│  │  │  ┌───────────────────────────┐  │ │ │
│  │  │  │  Overlay                  │  │ │ │
│  │  │  │  ┌─────────────────────┐  │  │ │ │
│  │  │  │  │  Content            │  │  │ │ │
│  │  │  │  │  - Title            │  │  │ │ │
│  │  │  │  │  - Subtitle         │  │  │ │ │
│  │  │  │  │  - Description      │  │  │ │ │
│  │  │  │  │  - Actions (Btns)   │  │  │ │ │
│  │  │  │  └─────────────────────┘  │  │ │ │
│  │  │  └───────────────────────────┘  │ │ │
│  │  └─────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  📦 <Section id="about" />            │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  <Container maxWidth="lg" />    │ │ │
│  │  │  ┌───────────────────────────┐  │ │ │
│  │  │  │  Conteúdo                 │  │ │ │
│  │  │  └───────────────────────────┘  │ │ │
│  │  └─────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  📦 <Section id="services" />         │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  <Container />                  │ │ │
│  │  │  ┌───────────────────────────┐  │ │ │
│  │  │  │  Grid Layout              │  │ │ │
│  │  │  │  ┌────┬────┬────┐         │  │ │ │
│  │  │  │  │Card│Card│Card│         │  │ │ │
│  │  │  │  │ └─ │ └─ │ └─ │         │  │ │ │
│  │  │  │  │<Img│<Img│<Img│         │  │ │ │
│  │  │  │  │<Btn│<Btn│<Btn│         │  │ │ │
│  │  │  │  └────┴────┴────┘         │  │ │ │
│  │  │  └───────────────────────────┘  │ │ │
│  │  └─────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  👟 <Footer />                        │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  Main Content                   │ │ │
│  │  │  ┌──────┬──────┬──────┬──────┐  │ │ │
│  │  │  │Col 1 │Col 2 │Col 3 │Social│  │ │ │
│  │  │  └──────┴──────┴──────┴──────┘  │ │ │
│  │  └─────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  Bottom Bar                     │ │ │
│  │  │  Copyright | Links              │ │ │
│  │  └─────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

## 🔗 Fluxo de Composição

```
1. Import
   ↓
┌──────────────────────────────────┐
│ import { Header, Section, etc }  │
│ from './components'              │
└──────────────────────────────────┘
   ↓
2. Compose
   ↓
┌──────────────────────────────────┐
│ <Header                          │
│   logo={...}                     │
│   navigation={...}               │
│   actions={...}                  │
│ />                               │
└──────────────────────────────────┘
   ↓
3. Render
   ↓
┌──────────────────────────────────┐
│ DOM Real Browser                 │
└──────────────────────────────────┘
```

## 🧩 Composição de Botões (Exemplo)

```
App.tsx
  └─> <HeroSection actions={...} />
        └─> <>
              <Button variant="primary">CTA 1</Button>
              <Button variant="secondary">CTA 2</Button>
            </>

Resultado Visual:
┌──────────────┐  ┌──────────────┐
│   CTA 1     │  │   CTA 2     │
│  (Primary)   │  │ (Secondary)  │
└──────────────┘  └──────────────┘
```

## 📦 Container System

```
<Section>                          ← Adiciona padding vertical
  <Container maxWidth="lg">        ← Limita largura e centraliza
    <div>Conteúdo</div>            ← Seu conteúdo
  </Container>
</Section>

Visual:
┌────────────────────────────────────────────┐ ← Section (100% width)
│  ↕ padding                                 │
│  ┌──────────────────────────────────────┐ │ ← Container (max-width)
│  │  ← padding → Conteúdo ← padding →    │ │
│  └──────────────────────────────────────┘ │
│  ↕ padding                                 │
└────────────────────────────────────────────┘
```

## 🎯 Sistema de Props

```
Component
    │
    ├─> Required Props  (obrigatórias)
    │   └─> ex: src, alt, children
    │
    ├─> Optional Props  (opcionais)
    │   └─> ex: variant, size, className
    │
    └─> Default Values  (valores padrão)
        └─> ex: variant="primary", size="medium"
```

## 🌈 Variantes de Componentes

### Button Variants
```
primary     → ┌──────────┐  Fundo colorido
              │  Click   │
              └──────────┘

secondary   → ┌──────────┐  Fundo transparente
              │  Click   │  Borda colorida
              └──────────┘

outline     → ┌──────────┐  Fundo transparente
              │  Click   │  Borda simples
              └──────────┘
```

### Image States
```
Loading     → ┌──────────┐  Skeleton animation
              │ ▓▓▓▓▓▓▓▓ │
              └──────────┘

Loaded      → ┌──────────┐  Imagem real
              │   [IMG]  │
              └──────────┘

Error       → ┌──────────┐  Fallback
              │   ⚠️     │
              └──────────┘
```

## 📱 Responsividade

```
Desktop (> 768px)
┌─────────────────────────────────┐
│ Header: Logo | Nav | Actions    │
├─────────────────────────────────┤
│         Hero Section             │
├─────────────────────────────────┤
│ Section: [Col1][Col2][Col3]     │
└─────────────────────────────────┘

Mobile (< 768px)
┌───────────────┐
│ Header: Logo  │
│   Nav (menu)  │
│   Actions     │
├───────────────┤
│  Hero Section │
├───────────────┤
│ Section:      │
│   [Column 1]  │
│   [Column 2]  │
│   [Column 3]  │
└───────────────┘
```

## 🎨 Sistema de Cores (CSS Variables)

```
:root {
  --color_11  → Background
  --color_15  → Text
  --color_17  → Primary
  --color_18  → Secondary
  --color_19  → Hover
}

Uso:
background-color: rgb(var(--color_11));
color: rgb(var(--color_15));
```

## 🔄 Ciclo de Vida dos Componentes

```
1. Import      → Importar componentes
2. Compose     → Montar estrutura
3. Props       → Passar configurações
4. Render      → React renderiza
5. Update      → Props/State mudam
6. Re-render   → React atualiza
```

## 📊 Métricas da Reorganização

```
✅ Componentes Criados:     8
✅ Arquivos CSS:            8
✅ Níveis de Hierarquia:    3 (layout, sections, ui)
✅ Documentação:            3 arquivos
✅ Padrão:                  Composition Pattern
✅ TypeScript:              100%
✅ Responsivo:              ✓
✅ Acessível:               ✓
```

---

**Este mapa visual ajuda a entender rapidamente a estrutura e organização do projeto!** 🎯
